# spec: /projets

> Status: deferred. This page is in scope but not yet specified.

The `/projets` page is planned but will not be implemented in the current iteration. It will be addressed in a dedicated spec when the content and structure are defined.

When specified, project entries will be rendered using the shared `Card` component, consistent with `/experiences` and `/veille`.

The route must not exist until the spec is written and the implementation is ready.