import site from '@/data/site.json';

export const IS_DEMO = site.isDemo;
export const siteUrl = site.siteUrl;
export const siteName = site.siteName;
export const defaultTitle = site.defaultTitle;
export const defaultDescription = site.defaultDescription;
export const defaultOgImage = site.defaultOgImage;
export const VEILLE_PREVIEW_COUNT = site.veillePreviewCount;
export const VEILLE_PAGE_SIZE = site.veillePageSize;
export const PROJECTS_PREVIEW_COUNT = site.projectsPreviewCount;
export const PROJECTS_PAGE_SIZE = site.projectsPageSize;
